--- a
+++ b
@@ -8,6 +8,8 @@ ENV RUSTUP_HOME=/usr/local/rustup \
     RUST_VERSION=%%RUST-VERSION%%
 
 RUN set -eux; \
+    apt-get -y purge liblzma-dev; \
+    apt-get -y autoremove; \
     %%ARCH-CASE%%; \
     url="https://static.rust-lang.org/rustup/archive/%%RUSTUP-VERSION%%/${rustArch}/rustup-init"; \
     wget "$url"; \
